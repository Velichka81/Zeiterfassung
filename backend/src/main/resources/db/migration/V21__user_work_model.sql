-- NO-OP: Columns already exist in current environment.
-- If you set up a fresh DB and these columns are missing, delete the database file (db/zeiterfassung.db)
-- and run migrations from scratch, or create a new V22 migration to add the missing columns.
SELECT 1;
